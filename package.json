{
  "name": "kingogm",
  "version": "0.1.0",
  "private": true,
  "description": "Gym management system monorepo",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev"
  }
}

